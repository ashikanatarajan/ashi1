#include<stdio.h>
#include<conio.h>
int main()
{
int n,x,p,m,k=1;
printf("Enter the value:");
scanf("%d",&n);
printf("Enter the pair:");
scanf("%d",&p);
for(m=0;m<2*n;m++)
{
x=2*n-k;
if(p!=x)
{
printf("The pair is%d for days %d is %d\n",p,j++,p);
}
}
getch();
}
